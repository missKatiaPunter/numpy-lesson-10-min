# A 10-minute lesson introducing Numpy Arrays

Pushed from [Deepnote](https://deepnote.com).

To isntall scikit-image:

```bash
python -m pip install -U scikit-image
```

> Numpy arrays are only more efficient for huge data sets
